<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800" MinHeight="600" MinWidth="800"
        WindowStartupLocation="CenterScreen" FontFamily="Bahnschrift SemiCondensed"
        FontSize="17"
        Loaded="Window_Loaded">
    <DockPanel>
        <Canvas Panel.ZIndex="100" DockPanel.Dock="Top">
            <StackPanel Orientation="Horizontal" Canvas.Left="165">
                <Button x:Name="BtnTeam" Click="Button_Click" Height="32" Background="White" BorderBrush="White">
                    <Button.Content>
                        <Image Source="https://cdn-icons-png.flaticon.com/512/1041/1041728.png" Width="32" ></Image>
                    </Button.Content>
                </Button>
                <Expander x:Name="exTeamSelected" Opacity=".9" Background="LightGreen" Header="Select team" Width="160" Padding="5" Margin="0 20 0 20">
                    <Expander.Content>
                        <StackPanel Margin="10">
                            <ComboBox x:Name="cbTeams" Margin="0 0 0 10" MaxDropDownHeight="200" />
                            <Button Click="ChangeTeam_Click" Content="Change team" Width="110" Padding="2">
                                <Button.Resources>
                                    <Style TargetType="{x:Type Border}">
                                        <Setter Property="CornerRadius" Value="7"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </Expander.Content>
                </Expander>
                <Label x:Name="lblResult" Content="x : x" VerticalAlignment="Center" FontSize="25" Padding="20 0 20 0"/>
                <Expander x:Name="exOpponent" Opacity=".9" Background="LightGreen" Header="Select opponent" Width="160" Padding="5" Margin="0 20 0 20">
                    <Expander.Content>
                        <StackPanel Margin="10">
                            <ComboBox x:Name="cbOpponent" Margin="0 0 0 10" MaxDropDownHeight="200"/>
                            <Button x:Name="btnOpponent" Click="ChangeOpponent_Click" Content="Select opponent" Width="120" Padding="2">
                                <Button.Resources>
                                    <Style TargetType="{x:Type Border}">
                                        <Setter Property="CornerRadius" Value="7"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </Expander.Content>
                </Expander>
                <Button x:Name="BtnOpponent" Click="Button_Click" Height="32" Background="White" BorderBrush="White">
                    <Button.Content>
                        <Image Source="https://cdn-icons-png.flaticon.com/512/1041/1041728.png" Width="32" ></Image>
                    </Button.Content>
                </Button>
            </StackPanel>
        </Canvas>
        <StackPanel DockPanel.Dock="Bottom">
            <Image Margin="0 65 0 0" Source="https://thumbs.dreamstime.com/b/soccer-field-football-background-green-court-pattern-lawn-148794562.jpg"/>
        </StackPanel>
    </DockPanel>
</Window>
